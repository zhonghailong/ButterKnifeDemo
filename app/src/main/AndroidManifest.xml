<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.demoapplication">
<uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".SinglTopActivity"
             android:launchMode="singleTop"></activity>
        <activity android:name=".SinglinstanceActivity"
            android:launchMode="singleInstance"></activity>
        <activity android:name=".SinglTaskActivity"
            android:launchMode="singleTask"></activity>
        <activity android:name=".okhttp.OkHttpActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>